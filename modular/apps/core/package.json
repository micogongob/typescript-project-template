{
    "name": "@local/core",
    "version": "0.0.0",
    "private": true,
    "main": "./dist/server.js",
    "scripts": {
        "build": "npm run ts-build",
        "ts-build": "rm -rf dist && tsc",
        "test": "npm run ts-mocha",
        "js-mocha": "nyc --reporter=html mocha 'tests/**/*.spec.js'",
        "ts-mocha": "nyc --reporter=html mocha -r ts-node/register 'tests/**/*.spec.ts'",
        "start": "npm run ts-run",
        "ts-run": "DEBUG=app,app:* nodemon -r dotenv/config ./src/index.ts",
        "lint": "eslint . --ext .ts",
        "fmt": "eslint . --ext .ts --fix",
        ".env": "cp -f .env.example .env"
    },
    "dependencies": {
        "@local/commons": "workspace:^",
        "express": "^4.18.2"
    },
    "devDependencies": {
        "@local/config-eslint": "workspace:^",
        "@local/config-typescript": "workspace:^",
        "@local/dev-dependencies": "workspace:^",
        "@types/express": "^4.17.17"
    }
}
